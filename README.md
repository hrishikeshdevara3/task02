##Advance EDA & Feature engineering:
Problem Statement : Given dataset is containg some null values and some of the values in data are duplicate.So the goal is to clean the dataset for the further processing of dataset through machine learning Algorithms.And also to a 3 new features to existing dataset where
Libraries:
Numpys,Pandas,Feature Scaling and Data cleaning
Solutions:
The solution of this is to first load the dataset by importing the csv file into the jupyter notebook where the project folder is creatd . First we need to findout what are the null constraints,duplicate values are present in the dataset after identfying we have to clean the data with revelant analysis method and after cleaning the dataset where each values beside the non-null it should be non-null.If it is correct then we should proccessed with the adding the feature to the data and at last we need to save the original dataset as (clean_dataset_csv) file after the cleaning and preproccessing methods.
